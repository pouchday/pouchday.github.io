{
  "name": "pouchday",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev dist --proxy 5173 -- npm run dev",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -m 'Auto-deploy production asset distribution [skip ci]'"
  },
  "devDependencies": {
    "alpinejs": "^3.15.12",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.12",
    "vite-plugin-html-inject": "^1.1.2",
    "wrangler": "^4.103.0"
  }
}